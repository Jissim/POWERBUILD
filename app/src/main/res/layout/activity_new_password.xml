<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/activity_forgot_password.xml -->

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"> <!-- Fond noir -->

    <ImageView
        android:id="@+id/logoImageView"
        android:layout_width="222dp"
        android:layout_height="207dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:contentDescription="@string/logo_description"
        android:src="@drawable/Logo" />

    <TextView
        android:id="@+id/instructionsTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/logoImageView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:text="Entrez votre adresse e-mail pour réinitialiser votre mot de passe."
        android:textColor="#FFFFFF"/>

    <EditText
        android:id="@+id/emailEditTextForgot"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/instructionsTextView"
        android:layout_marginTop="20dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:hint="Email"
        android:inputType="textEmailAddress"
        android:textColor="#FFFFFF"
        android:padding="10dp"
        android:background="#A0A0A0"/> <!-- Fond gris clair -->

    <Button
        android:id="@+id/resetPasswordButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/emailEditTextForgot"
        android:layout_marginTop="20dp"
        android:text="Réinitialiser le mot de passe"
        android:textColor="#FFFFFF"
        android:background="#FF0000"/> <!-- Fond rouge -->

</RelativeLayout>
